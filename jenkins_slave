pipeline {
    agent any
    
    stages {
        stage('Example') {
            steps {
                sh ("python myscript.py firstName ${params.FIRSTNAME} height ${params.HEIGHT} weight ${params.WEIGHT} age ${params.AGE}")
            }
        }
    }
}
